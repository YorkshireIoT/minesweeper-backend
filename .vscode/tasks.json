{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "gcc main.c src/minesweeper.c -Iinclude -Wall -g -o main.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}